const divArr = ['div', 'li', 'div', 'input', 'label']
const elName = ['removeBtnDiv', 'newList', 'liContextDiv', 'checkDiv', 'checkLabel']

for (let v in divArr)
{
    const elName[0] =   
}


[].map(filtered)

function filtered(value, index, array) {
    return {
        value: value + '1'
    }
}
